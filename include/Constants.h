#pragma once

// Screen dimensions
const int SCREEN_WIDTH = 960;
const int SCREEN_HEIGHT = 640;

// Rendering constants
const float HORIZON_Y = 320.0f;
const float CAMERA_HEIGHT = 1000.0f;

// Track constants
const int NUM_SEGMENTS = 300;
const float SEGMENT_LENGTH = 200.0f;
const float TRACK_LENGTH = NUM_SEGMENTS * SEGMENT_LENGTH;